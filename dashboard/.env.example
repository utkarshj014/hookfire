# ==============================================================================
# Hookfire - Frontend React Dashboard Environment Variables
# ==============================================================================

# Target Express API URL (consumed by client axios requests)
# Note: Keep this as localhost:3000 even in Docker Compose since Vite compiles code 
# that runs inside the user's host browser.
VITE_API_URL="http://localhost:3000"
