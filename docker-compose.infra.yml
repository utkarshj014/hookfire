services:
  redis:
    image: redis:8-alpine
    container_name: hookfire-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-my-redis-secure-password}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
