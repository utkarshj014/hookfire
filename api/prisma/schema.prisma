generator client {
  provider = "prisma-client-js"
}

datasource db { 
  provider = "postgresql"
}

model Event {
  id        String   @id @default(cuid())
  eventType String
  payload   Json
  createdAt DateTime @default(now())

   @@map("event")
}